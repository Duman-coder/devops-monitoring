## Ansible — Автоустановка проекта

Автоматически устанавливает Docker и запускает проект на любом Ubuntu сервере.

### Запуск

```bash
ansible-playbook -i ansible/inventory.ini ansible/playbook.yml
```
