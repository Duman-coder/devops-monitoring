apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ping-monitor.fullname" . }}-test-connection"
  labels:
    {{- include "ping-monitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ping-monitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
