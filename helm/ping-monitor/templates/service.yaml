apiVersion: v1
kind: Service
metadata:
  name: {{ include "ping-monitor.fullname" . }}
  labels:
    {{- include "ping-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ping-monitor.selectorLabels" . | nindent 4 }}
